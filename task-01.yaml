# 14-MAY-2021, preparing the 2 switches with L2, bonding, bridge configuration
# 14-MAY-2021, hands-on via task-001.yaml and should include spine3 and leaf1

- hosts: leaf1
  name: startup
  become: yes
  gather_facts: no
  tasks:
    - name: copy leaf1 task-01.yaml
      copy:
        src: /home/cumulus/ON-10/leaf1-task-01.yaml
        dest: /etc/cue.d/startup.yaml
    # aktivieren
    - name: activate changes for leaf1
      shell: cl config apply startup -y
      
- hosts: leaf2
  name: startup
  become: yes
  gather_facts: no
  tasks:
    - name: copy leaf2 task-02.yaml
      copy:
        src: /home/cumulus/ON-10/leaf2-task-02.yaml
        dest: /etc/cue.d/startup.yaml
    # aktivieren
    - name: activate changes for leaf2
      shell: cl config apply startup -y   
      
- hosts: spine3
  name: startup
  become: yes
  gather_facts: no
  tasks:
    - name: copy leaf1 task-01.yaml
      copy:
        src: /home/cumulus/ON-10/spine3-task-01.yaml
        dest: /etc/cue.d/startup.yaml
    # aktivieren
    - name: activate changes for spine3
      shell: cl config apply startup -y
      
- hosts: spine4
  name: startup
  become: yes
  gather_facts: no
  tasks:
    - name: copy leaf1 task-01.yaml
      copy:
        src: /home/cumulus/ON-10/spine4-task-01.yaml
        dest: /etc/cue.d/startup.yaml
    # aktivieren
    - name: activate changes for spine4
      shell: cl config apply startup -y
      
